#include <stdio.h>
/**
 * main - Prints all single digit numbers of base 10 starting
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	printf("%i", n);

	putchar('\n');

	return (0);
}
